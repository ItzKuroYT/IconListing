{
  "name": "icon-listing",
  "version": "1.0.0",
  "private": true,
  "description": "A lightweight Minecraft server listing website for GitHub Pages with a single Vercel serverless API.",
  "scripts": {
    "dev": "vercel dev",
    "static": "node dev-server.js",
    "check": "node -c api/index.js && node -c assets/js/app.js && node -c dev-server.js && node -c scripts/generate-server-pages.js",
    "generate:server-pages": "node scripts/generate-server-pages.js",
    "smoke": "node scripts/smoke-test.js"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^34.3.0"
  }
}
